const router = require('express').Router();
const { Project, User } = require('../models');

// get and render
router.get('/', async (req, res) => {
  res.render('index.html')
})

// get by id and render

// Get user 



